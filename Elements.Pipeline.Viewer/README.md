# Elements Pipeline Viewer

Three.js client for the Elements pipeline scene service.

```powershell
npm install
npm run dev
```

The Vite development server proxies `/api` to `http://localhost:5188`. A production build is written to `Elements.Pipeline.Server/wwwroot` and is served by ASP.NET Core.
